/**
 * The one address the magazine answers on. Written here rather than in each
 * place that needs it, because a dead contact address is the kind of thing a
 * site carries for a year without anyone noticing.
 */
export const CONTACT_EMAIL = "hello@glitchdecoded.com";
